# =============================================================================
# 全项目单一 Python 虚拟环境（后端 + AI-ASR + ai-vpr-ser）
# =============================================================================
# 建议在仓库根目录创建 .venv，只安装本文件一次，避免多份 torch / librosa。
#
#   python -m venv .venv
#   .venv\Scripts\python -m pip install -U pip
#   .venv\Scripts\pip install -r requirements.txt
#
# 启动各服务时请先 cd 到对应子目录，再使用根目录解释器，例如：
#   cd backend
#   ..\.venv\Scripts\python run_server.py
#
# Python 版本：3.10+ 推荐（3.9 需自行验证 torch/speechbrain 轮子）。
#
# 可选 FunASR（体积大、Windows 可能需 MSVC）：在已安装本文件后执行
#   pip install -r ai-asr/requirements-funasr.txt
# =============================================================================

# ---------- Web / 后端 ----------
fastapi>=0.110,<1
uvicorn[standard]>=0.27,<1
python-multipart>=0.0.9
httpx>=0.27,<1
sqlalchemy>=2.0.25,<3
bcrypt>=4.1,<5
python-jose[cryptography]>=3.3,<4
pydantic>=2.10,<3
pydantic-core>=2.27,<3
pydantic-settings>=2,<3
typing_extensions>=4.8

# ---------- 数值 / 音频（ASR + VPR 共用）----------
numpy>=1.23,<3
scipy>=1.9
librosa>=0.10
soundfile>=0.12
noisereduce>=3

# ---------- PyTorch（Whisper + SpeechBrain 共用同一份 torch）----------
torch>=2.1,<3
torchaudio>=2.1,<3

# ---------- ASR：Whisper 路线（默认）----------
openai-whisper

# ---------- ai-vpr-ser：声纹 + 情感 ----------
speechbrain>=1,<2
huggingface_hub>=0.20,<1
# 情感备用：SpeechBrain 官方 IEMOCAP 模型在部分环境会强依赖可选包 k2，用 transformers 管道兜底
transformers>=4.40,<5
